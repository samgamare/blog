# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Sam's Thoughts
#email: gamare.blog@gmail.com
#description: >- # this means to ignore newlines until "baseurl:"
  
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.gamare.net" # the base hostname & protocol for your site, e.g. http://example.com
#twitter_username: jekyllrb
#github_username:  jekyll

collections:
  posts:
    output: true
    permalink: /blog/:year/:month/:day/:title/ 

  # Add this block for your new collection
  labs:
    output: true            # Tells Jekyll to generate a file for each project
    permalink: /labs/:year/:month/:day/:title/ 

show_excerpts: true
show_subscribe: false

# Build settings
theme: minima
#plugins:
#  - jekyll-search
#  - jekyll-feed

# Exclude from processing.
#
# Jekyll copies any unrecognised top-level file or directory straight into
# _site/, which means anything listed here would otherwise be published to the
# public website.
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - README.md
  # CI config, not site content
  - .github/
